import Filters from './Filters'

// eslint-disable-next-line react/prop-types
function Header () {
  return (
    <header>
      <h1>React Shop</h1>
      <Filters />
    </header>
  )
}

export default Header
